-- vim: ts=4 sw=4 noet ai cindent syntax=lua
--[[
Conky configuration for Netdog, alpha release
]]

conky.config = {
    out_to_console = true,
    total_run_times = 1,
}

conky.text = [[{
    "uptime": "$uptime",
    "frequency": "$freq MHz",
    "memory": "$mem/$memmax",
    "cpu_usage": "$cpu%",
    "fs_usage": "$fs_used/$fs_size",
}]]
